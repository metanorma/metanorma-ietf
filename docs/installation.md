---
title: Installation
---

Add this line to your application’s Gemfile:

```
gem "metanorma-ietf"
```

And then execute:

```
$ bundle
```

Or install it yourself as:

```
$ gem install metanorma-cli
```
