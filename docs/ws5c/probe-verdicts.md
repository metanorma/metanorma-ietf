# WS5c probe wave verdicts (banked 2026-08-08 19:27)
Scripts + stage dumps: session scratchpad ws5c/probes/ (probeN.sem/pres/rfc.xml; probe 3 crashes before serialisation).

- **P1 display-text (row 2): SURVIVES end-to-end** — the presentation stage rewrites authored xref/eref text into fmt-xref/semx and the transformer reads the presented form; custom text verbatim in body output for both internal xref and bibliographic eref. The model-level drop is fully masked on this path. → Row 2 CLOSES: no user-visible loss; C1's model-level observation recorded as masked-by-design.
- **P2 termnote/termexample (row 3): CONFIRMED dropped** — two-paragraph termnote → empty `<aside/>`; termexample → label "EXAMPLE 1" only (intro paragraph + bullet list gone). Presentation carried everything; loss at model parse (paragraph-shaped classes). → Extend #300 per ruling; model-blocked component for the metanorma-document follow-up.
- **P3 concept carriers (row 6): WORSE THAN AUDITED — CRASHES the conversion.** Presentation renders both concept flavors fine but retains the semantic `<concept>` sibling; the paragraph's element_order includes tag `concept`; `build_interleaved_content` has no INLINE_TAG_MAP entry and falls through to `safe_append(text_elem, :concept, …)` → NoMethodError on Rfcxml::V3::Text — unrescued, aborts the WHOLE document. Any body-paragraph concept is fatal (corpus/suite never exercise one outside terms sections). → Own ticket (draft 17), severity: conversion abort.
- **P4 non-verbal term definitions (row 9): CONFIRMED empty term sections** — term renders `<name>` only; sourcecode and formula definitions absent; presentation carried them; loss at model parse (nonverbalrepresentation unmapped). → AWAITING USER RULING (deliberate omission vs model-blocked fix).

Rulings landed earlier today (rows): 1 new ticket (pseudocode); 5 split — td/th/tr→#295, term→#300, remainder→#291 note; 7 enhancement ticket (width/height); 8 fold #295; 10 closed deliberate (rationale recorded); 11 ticket (erefstack); 12 amend closed N-A, inline date reclassified MODEL-BLOCKED line item with the 0.5.x batch (both-routes note: map fmt-date for i18n text; value-fallback for format-less dates).
User directive: the new metanorma-document follow-up ticket gets a SEPARATE comment: "Holding up the metanorma-ietf model-driven transformer — parent tracking issue: metanorma/metanorma-ietf#233." (same as the five stalled tickets).
